export type TagType = {
  tagId: number;
  tagName: string;
  parentTag: string | null;
};

export type AssetType = {
  ascId: number;
  ascName: string;
  balance: number;
};

export type BudgetType = {
  bscId: number;
  bscName: string;
};
